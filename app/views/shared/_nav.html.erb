<div class="navbar navbar-default header" role="navigation">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      </button>
      <span class="pull-left head-size logo">
        <%= link_to root_path do %>
          <i class="fa fa-instagram" aria-hidden="true"></i> | 
        <% end %>
        <strong><span class="brand"><%= link_to 'Instapost', root_path %></span></strong>
      </span>
    </div>
    <div class="navbar-collapse collapse">
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <li><%= link_to ' New Post', new_post_path, class: 'fa fa-pencil-square-o' %></li>
          <li class="dropdown nav-notify" data-behavior="notifications" data-notifications='<%= render template: "notifications/index", formats: [:json] %>'>
            <a href="#" class="dropdown-toggle" data-behavior="notifications-link" data-toggle="dropdown"><span class="glyphicon glyphicon-bell"></span><sup><span class="badge" data-behavior="unread-count"></span></sup><b class="caret"></b></a>
            <ul class="dropdown-menu" data-behavior="notification-items">
              <!-- notification-items -->
            </ul>
          </li>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              <% if current_user.photo? %>
                <span class="header_pic_small">
                  <%= cl_image_tag current_user.photo, :secure => true, alt: current_user.name.split[0], title: current_user.name %>
                </span>
              <% else %>
                <span class="header_pic_small">
                  <%= image_tag "https://goo.gl/2wWxZy" %>
                </span>
              <% end %>
              <b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <li class="dropdown-header"><%= current_user.name %></li>
              <li><%= link_to 'Admin Area', admin_root_path if current_user.admin? %></li>
              <li><%= link_to "Timeline", timeline_path %></li>
              <li><%= link_to 'Profile', user_path(current_user.id) %></li>
              <li class="divider"></li>
              <li><%= link_to 'Log Out', destroy_user_session_path, method: :delete %></li>
            </ul>
          </li>
        <% else %>
          <li><%= link_to 'Sign Up', new_user_registration_path %></li>
          <li><%= link_to 'Log In', new_user_session_path %></li>
        <% end %>
      </ul>
    </div>
  </div>     
</div>
